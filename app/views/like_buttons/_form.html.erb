<%= form_for(@like_button) do |f| %>
  <% if @like_button.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@like_button.errors.count, "error") %> prohibited this like_button from being saved:</h2>

      <ul>
      <% @like_button.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :label %><br />
    <%= f.text_field :label %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
